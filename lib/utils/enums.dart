enum reqStatus { Accept, Decline }

enum QRStatus { Red, Green, NoColor }

enum ParticipantScreenMode { Create, Edit }

enum MailingScreenMode { All, Specific }

enum ExportScreenMode { JSON, CSV }

enum ParticipantType { All, Present, Absent }
